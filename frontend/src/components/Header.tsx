import React from 'react';

const Header = () => (
  <header className="text-center mb-8">
    <h1 className="text-4xl sm:text-5xl font-extrabold tracking-tight bg-clip-text text-transparent bg-gradient-to-r from-cyan-400 to-blue-600">
      Gemini Proxy Frontend
    </h1>
    <p className="text-gray-400 mt-2 text-lg">Privileged executor running in the Google AI Studio environment.</p>
  </header>
);

export default Header;
