import React from 'react';

const Footer = () => (
    <footer className="mt-6 text-center text-gray-500 text-xs sm:text-sm flex-shrink-0">
        <p>This frontend is ready to execute commands. Send API requests to your proxy backend service.</p>
    </footer>
);

export default Footer;
